spring:
  application:
    name: NextClass
  datasource:
    #url: jdbc:postgresql://localhost:5432/nextclass
    url: jdbc:h2:mem:nextclass
    #driver-class-name: org.postgresql.Driver
    driver-class-name: org.h2.Driver
    #username: postgres
    #password: postgres
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
      #ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    #database-platform: org.hibernate.dialect.PostgreSQLDialect

nextclass:
  security:
    secret: "2Gapq5r7J35hMtRqELHMQqURrSH7nQs80Lzj10DhnfU"